clc;
clear;
t = linspace(0,0.1,10);

y0 = 0.15;
omega = sqrt(2);
theta = 0;

% Case 1:
zeta = 3 / (2 * sqrt(2));
%y = y(y0, zeta, omega, t, theta);
y = t;
for i = 1:100
    y(i) = y(y0, zeta, omega, t(i), theta);
end
disp(y)
